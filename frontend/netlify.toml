[build]
  publish = "frontend"  # only if publish is frontend; otherwise publish="."

[[redirects]]
  from = "/api/*"
  to = "https://medical-narration.onrender.com/api/:splat"
  status = 200
  force = true
